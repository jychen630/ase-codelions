/**
 * Search services and utilities for tweets.
 *
 * <p>This package contains an in-memory / DB-backed search service that
 * supports simple boolean (AND/OR) and phrase matching, plus hashtag
 * search. The data source is selected by the flag
 * {@code app.search.source} (values: {@code memory} or {@code db}).
 */
package com.team.mcp.search;
