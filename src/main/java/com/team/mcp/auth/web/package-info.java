/**
 * Web endpoints for the authentication / OAuth demo flow.
 *
 * <p>Iteration-1 provides a simulated OAuth start + callback so we can
 * store tokens in the {@code TokenStore} without calling Twitter yet.
 */
package com.team.mcp.auth.web;
