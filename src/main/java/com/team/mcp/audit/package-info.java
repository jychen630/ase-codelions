/**
 * Audit entities and repositories for MCP tool-call logging.
 * <p>
 * Contains JPA entities and related types that persist metadata about
 * each JSON-RPC {@code tools/call} invocation made through the MCP server.
 */
package com.team.mcp.audit;
