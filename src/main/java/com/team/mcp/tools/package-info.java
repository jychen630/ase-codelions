/**
 * HTTP-facing controllers that expose minimal, tool-shaped endpoints for
 * Iteration-1 (timeline reading and tweet scheduling). These endpoints are
 * used by simple API tests and can later be replaced or complemented by
 * MCP stdio/WebSocket adapters.
 */
package com.team.mcp.tools;
